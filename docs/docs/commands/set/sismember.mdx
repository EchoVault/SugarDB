import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# SISMEMBER

### Syntax
```
SISMEMBER key member
```

### Module
<span className="acl-category">set</span>

### Categories 
<span className="acl-category">fast</span>
<span className="acl-category">read</span>
<span className="acl-category">set</span>

### Description 
Returns if member is contained in the set.

### Examples

<Tabs
  defaultValue="go"
  values={[
    { label: 'Go (Embedded)', value: 'go', },
    { label: 'CLI', value: 'cli', },
  ]}
>
  <TabItem value="go">
    Check if a member is in the set:
    ```go
    db, err := sugardb.NewSugarDB()
    if err != nil {
      log.Fatal(err)
    }
    ok, err := db.SisMember("key", "member")
    ```
  </TabItem>
  <TabItem value="cli">
    Check if a member is in the set:
    ```
    > SISMEMBER key member
    ```
  </TabItem>
</Tabs>
